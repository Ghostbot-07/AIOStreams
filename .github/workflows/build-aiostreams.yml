name: Build AIOStreams (frontend + backend, no cache)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ─────────────────────────────────────────────
      # 1. Checkout & Versioning
      # ─────────────────────────────────────────────
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷️ Fetch upstream tags
        run: git fetch https://github.com/viren070/aiostreams.git --tags

      - name: 🏷️ Resolve latest STABLE release tag
        id: version
        run: |
          STABLE_TAG=$(git tag --list \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
            | sort -V \
            | tail -n 1)

          if [ -z "$STABLE_TAG" ]; then
            echo "❌ No stable tag found"
            exit 1
          fi

          echo "Using stable tag: $STABLE_TAG"
          echo "version=$STABLE_TAG" >> $GITHUB_OUTPUT

      - name: 🔁 Checkout stable tag
        run: git checkout ${{ steps.version.outputs.version }}

      # ─────────────────────────────────────────────
      # 2. Setup Environment
      # ─────────────────────────────────────────────
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 📦 Install dependencies
        run: pnpm install

      # ─────────────────────────────────────────────
      # 3. Build Everything
      # ─────────────────────────────────────────────
      - name: 🧱 Build core
        run: pnpm --filter @aiostreams/core run build

      - name: ⚙️ Build server
        run: pnpm --filter @aiostreams/server run build

      - name: 🎨 Build frontend (static export)
        run: pnpm --filter @aiostreams/frontend run build

      # ─────────────────────────────────────────────
      # 4. Generate Metadata
      # ─────────────────────────────────────────────
      - name: 🏷️ Generate build metadata
        run: |
          mkdir -p resources
          cat <<EOF > resources/metadata.json
          {
            "version": "${{ steps.version.outputs.version }}",
            "environment": "production",
            "gitCommit": "$(git rev-parse HEAD)",
            "buildTime": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")",
            "commitTime": "$(git show -s --format=%cI HEAD)"
          }
          EOF

      # ─────────────────────────────────────────────
      # 5. Assemble Artifact (Locally)
      # ─────────────────────────────────────────────
      - name: 📦 Prepare final artifact structure
        run: |
          mkdir -p final-artifact/packages/core
          mkdir -p final-artifact/packages/server
          mkdir -p final-artifact/packages/frontend
          mkdir -p final-artifact/resources

          cp -r packages/core/dist final-artifact/packages/core/
          cp -r packages/server/dist final-artifact/packages/server/
          cp -r packages/frontend/out final-artifact/packages/frontend/
          cp resources/metadata.json final-artifact/resources/

      # ─────────────────────────────────────────────
      # 6. Upload
      # ─────────────────────────────────────────────
      - name: ⬆️ Upload artifact (24h retention)
        uses: actions/upload-artifact@v4
        with:
          name: aiostreams-${{ steps.version.outputs.version }}
          # Using 'final-artifact/*' tells GitHub: 
          # "Take everything INSIDE this folder and put it at the root of the zip"
          path: final-artifact/*
          retention-days: 1
